@Library("InSearchOfName's-Library") _
pipeline {
	agent {
		label 'docker-agent'
	}
	stages {
		stage('Checkout') {
			steps {
				checkout scm
			}
		}
		stage('Build') {
			steps {
				script {
					java.buildGradle(tasks: 'clean build')
				}
			}
		}
	}
}
